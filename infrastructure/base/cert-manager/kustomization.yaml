---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - cert-manager.yaml
  # Cluster-specific secrets and ClusterIssuer live in overlays (e.g. infrastructure/talos/clusterissuer)
